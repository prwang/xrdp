# Bisect D: CBR only, no timing SEI
# RDP endpoint on the host: 127.0.0.1:40003 (tester / host tester password).
# Image tag pins the xrdp-dev commit; gfx.toml comes from the committed
# gfx/arm-d.toml via ConfigMap xrdp-gfx-arm-d (created by build_and_deploy.sh).
# Privileged + /dev/dri hostPath: the pod's ffmpeg encodes via VAAPI on the
# host GPU, same as the host baseline instance.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xrdp-arm-d
  namespace: bisect-matrix
  labels:
    app: xrdp-bisect
    arm: arm-d
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      arm: arm-d
  template:
    metadata:
      labels:
        app: xrdp-bisect
        arm: arm-d
    spec:
      containers:
        - name: xrdp
          image: localhost/xrdp-bisect:52099149
          imagePullPolicy: Never
          securityContext:
            privileged: true
          env:
            - name: ARM_LABEL
              value: "D: CBR only, no timing SEI  (port 40003)"
          ports:
            - containerPort: 3389
              hostPort: 40003
              hostIP: 127.0.0.1
              protocol: TCP
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm
            - name: dri
              mountPath: /dev/dri
            - name: matrix-cred
              mountPath: /run/matrix
              readOnly: true
            - name: gfx
              mountPath: /etc/xrdp/gfx.toml
              subPath: gfx.toml
            - name: banner
              mountPath: /usr/local/bin/banner.sh
              subPath: banner.sh
      volumes:
        - name: dshm
          # k8s defaults /dev/shm to 64Mi; the packed-view AVC444
          # capture shmem (two slots x two views, all monitors) needs
          # ~74MB for a dual-4K layout -- an undersized tmpfs SIGBUSes
          # Xorg mid-session (2026-07-28 multimon incident)
          emptyDir:
            medium: Memory
            sizeLimit: 512Mi
        - name: dri
          hostPath:
            path: /dev/dri
        - name: matrix-cred
          hostPath:
            path: /etc/xrdp-matrix
            type: Directory
        - name: banner
          configMap:
            name: xrdp-banner
            defaultMode: 0o755
        - name: gfx
          configMap:
            name: xrdp-gfx-arm-d
