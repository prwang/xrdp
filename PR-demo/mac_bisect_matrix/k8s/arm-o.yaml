# Arm N: FR-H264-8 EXPERIMENTAL LTR aux-chain (aux_ltr_chain=true) on the
# arm-m VAAPI CQP 444 config -- the acceptance-gate A/B arm vs arm-m.
# RDP endpoint on the host: 127.0.0.1:40014 (tester / host tester password).
# Image tag pins the xrdp-dev commit; gfx.toml comes from the committed
# gfx/arm-o.toml via ConfigMap xrdp-gfx-arm-o (created by build_and_deploy.sh).
# Privileged + /dev/dri hostPath: the pod's ffmpeg encodes via VAAPI on the
# host GPU, same as the host baseline instance.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xrdp-arm-o
  namespace: bisect-matrix
  labels:
    app: xrdp-bisect
    arm: arm-o
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      arm: arm-o
  template:
    metadata:
      labels:
        app: xrdp-bisect
        arm: arm-o
    spec:
      containers:
        - name: xrdp
          image: localhost/xrdp-bisect:34795577580b.xx5b9650c-xfce
          imagePullPolicy: Never
          securityContext:
            privileged: true
          env:
            - name: SESSION_KIND
              value: "xfce"
            - name: ARM_LABEL
              value: "O: re-key boundary (ltr_rekey_frame_num=536) + LTR aux-chain  (port 40014)"
          ports:
            - containerPort: 3389
              hostPort: 40014
              hostIP: 127.0.0.1
              protocol: TCP
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm
            - name: dri
              mountPath: /dev/dri
            - name: matrix-cred
              mountPath: /run/matrix
              readOnly: true
            - name: gfx
              mountPath: /etc/xrdp/gfx.toml
              subPath: gfx.toml
            - name: banner
              mountPath: /usr/local/bin/banner.sh
              subPath: banner.sh
            - name: banner
              mountPath: /usr/local/share/code_corpus.ansi
              subPath: code_corpus.ansi
      volumes:
        - name: dshm
          # k8s defaults /dev/shm to 64Mi; the packed-view AVC444
          # capture shmem (two slots x two views, all monitors) needs
          # ~74MB for a dual-4K layout -- an undersized tmpfs SIGBUSes
          # Xorg mid-session (2026-07-28 multimon incident)
          emptyDir:
            medium: Memory
            sizeLimit: 512Mi
        - name: dri
          hostPath:
            path: /dev/dri
        - name: matrix-cred
          hostPath:
            path: /etc/xrdp-matrix
            type: Directory
        - name: banner
          configMap:
            name: xrdp-banner
            defaultMode: 0o755
        - name: gfx
          configMap:
            name: xrdp-gfx-arm-o
