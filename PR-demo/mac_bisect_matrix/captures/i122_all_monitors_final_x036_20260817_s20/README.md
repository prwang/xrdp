# #122 two-active control

This is the retained two-active control for #122. `TEXTFLOOD_MONITOR=all`
rendered the full 6400x2400 desktop. Surface 0 carried 629 damage events and
surface 1 carried 628. The paired decision record is
`docs/experiments/122-one-active-monitor.md`.

The exact image, packages, configuration, modelines, target identity,
certificate, trace sufficiency and distributions are archived in this
directory. `VERDICT.txt` reports a 1.32x producer margin, so internal-stage
overlap claims are not permitted; the same-payload regression comparison is.
