RED demonstration for the step-2 observed-vs-requested ratchet
(test_ltr_schedule_observed_vs_requested).

This ratchet could not be run against the pre-#45 commit: the state it
drives (xrdp_h264_ltr_state.refresh_period / pic_index[]) did not exist
there, so the test would not compile. It was demonstrated RED instead
against the step-2 code with ONLY the check disabled -- the one line

    if (st->refresh_period > 0 && st->started)

forced to false in xrdp_h264_annexb.c, everything else identical:

not ok 25 - test_avc444_ltr.c:avc444_ltr_dpb:test_ltr_schedule_observed_vs_requested: Assertion 'ltr_cut_feed(&st, 9, buf, (int)sizeof(buf), ((void *)0)) != 0' failed: ltr_cut_feed(&st, 9, buf, (int)sizeof(buf), ((void *)0)) == 0, 0 == 0
96%: Checks: 26, Failures: 1, Errors: 0

The failing assertion is the aux picture where the declared schedule
says a cut is due and a P arrived: without the check the rewriter ships
it (rv == 0). With the check it fails the pair, which is what
FR-H264-6 layer 3 requires -- a silently skipped refresh must never
reach the client.
