# #79 -- three explicit frontiers

This record replaces the instrumented delay-sweep narrative on 2026-08-17.
The fleet captures used xorgxrdp's synchronous per-frame `ACK_TRACE cap`
logger and were deleted under the instrument-on-path rule.

The retained result is the specified frontier model: capture-slot release,
server transaction progress and client display progress are separately named
and paired by identity, including per-monitor region ownership. Serialization
and arithmetic are deterministic test anchors. No delay or throughput number
from the deleted sweep is quotable. BACKLOG #124 owns live qualification.

See `docs/experiments/121-evidence-admissibility-cleanup.md`.
